import React from "react"
import styled from "styled-components"

const Landing = () => (
    <section id="#" style={{margin: `800px 0px`}}>
      <h1> this is landing! </h1>
    </section>
)

export default Landing